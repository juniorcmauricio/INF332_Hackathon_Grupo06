# Copy this file to movieapi.env and fill in your own keys and preferences.
# Do NOT commit movieapi.env with real secrets.

# App
HACK_APP_NAME=Hackathon Movies API
HACK_REQUEST_TIMEOUT_S=15.0

# TMDB (The Movie Database)
# v3 API key (required)
HACK_TMDB_V3_KEY=YOUR_TMDB_V3_API_KEY
# Base URL (usually leave as default)
HACK_MOVIES_API_BASE=https://api.themoviedb.org/3
# Watch providers (optional)
# Set to true to fetch streaming providers for each recommendation
HACK_TMDB_INCLUDE_PROVIDERS=false
# ISO 3166-1 country code used for providers (e.g., BR, US, PT)
HACK_TMDB_REGION=BR

# AI mode: remote | local | off (use "remote" for Gemini API)
HACK_AI_MODE=remote

# Google Gemini (recommended)
# API key is required when HACK_AI_MODE=remote
HACK_GEMINI_API_KEY=YOUR_GOOGLE_GEMINI_API_KEY
# Optional: force a specific model (examples: gemini-2.0-flash, gemini-1.5-flash)
# If not set, the app will auto-fallback among available models
# HACK_GEMINI_MODEL=gemini-2.0-flash
# Optional: custom prompt template. Placeholders: {mood}, {labels}, {top_k}
# HACK_GEMINI_PROMPT_TEMPLATE=Return ONLY a pure JSON array (no text, no code fences) with the top {top_k} exact genre names from: {labels}. Mood: {mood}.

# (Hugging Face legacy removed)
